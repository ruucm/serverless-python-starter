{
  "name": "by-downloader-python-api",
  "description": "",
  "version": "0.1.0",
  "scripts": {
    "start": "serverless offline start --port 3002 --noTimeout",
    "deploy": "sls deploy",
    "forever": "forever start -c 'npm run start' ./"
  },
  "dependencies": {
    "serverless-python-requirements": "^3.0.1"
  },
  "devDependencies": {
    "serverless-offline": "^3.18.0",
    "serverless-offline-python": "^3.22.1",
    "serverless-python-requirements": "^3.0.1"
  }
}
